# 复制本文件为 keystore.properties（去掉 .example），并按实际填写。
# keystore.properties 与 *.jks 均已被 .gitignore 忽略，不会入库。
#
# storeFile 路径相对于 app/ 模块目录解析（build.gradle 里用了 file(...)），
# 推荐把 webchat.jks 放在工程根目录，写法如下：

storeFile=../webchat.jks
storePassword=你的存储密码
keyAlias=webchat
keyPassword=你的key密码
